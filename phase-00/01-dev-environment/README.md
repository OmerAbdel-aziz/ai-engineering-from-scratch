# 01 — Dev Environment

- Opencode installed and configured as the coding assistant
- LLM provider connected via `opencode.json`
- `AGENTS.md` created with standing rules
- Reference clone placed at `~/aieng-reference/` (read-only)
- Repo structure set up: per-lesson directories mirroring the curriculum
- All Required dependcies are installed and verified 
