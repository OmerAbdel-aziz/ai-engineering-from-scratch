# 02 — Git & Collaboration

- Local repo initialized and pushed to GitHub (default branch: `main`)
- `.gitignore` configured with ML-relevant patterns:
  - Model weights: `.pt`, `.pth`, `.safetensors`
  - Environment: `.venv`, `.opencode`
- `AGENTS.md` codifies rules around the reference clone and lesson structure
