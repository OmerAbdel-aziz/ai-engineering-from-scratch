# Standing rules

## Reference clone is read-only

The curriculum reference lives at `/home/omer/aieng-reference/`. It is a **read-only reference**:

- Never write to, move, or delete any file or directory inside `/home/omer/aieng-reference/`.
- The reference is for reading lesson instructions and solution sketches only.
- When I ask you to "refine" or "fix" a lesson in my repo, my repo's existing file is always the file being edited — never overwrite it with the reference's version.
- If you are ever unsure whether a path belongs to my repo (`/home/omer/AI-engineering-from-scratch/`) or the reference clone (`/home/omer/aieng-reference/`), stop and ask.

## Lesson naming

Lesson directories mirror the curriculum numbering:
`phase-XX/YY-lesson-name/`
