# 04 — APIs & Keys

- `.env.example` created at repo root as a template for required environment variables
- API keys configured for the LLM provider (not committed — added to `.gitignore`)
- `opencode.json` configured with the provider connection
